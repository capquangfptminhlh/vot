import { api, requireUser } from "../core/backend.js";
function uuid(value){const id=String(value||"").trim();if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(id))throw new Error("Mã tin không hợp lệ.");return id;}
export async function favoriteState(listingId){await requireUser();const data=await api(`/listings/${uuid(listingId)}/favorite`,{auth:true});return Boolean(data);}
export async function toggleFavorite(listingId){await requireUser();const data=await api(`/listings/${uuid(listingId)}/favorite/toggle`,{method:"POST",auth:true});return Boolean(data.active);}
export async function reportListing(listingId,reason,detail=""){await requireUser();const allowed=new Set(["fake","scam","wrong_condition","wrong_product","prohibited","spam","other"]);const clean=String(reason||"").trim().toLowerCase();if(!allowed.has(clean))throw new Error("Lý do báo cáo không hợp lệ.");return api(`/listings/${uuid(listingId)}/reports`,{method:"POST",auth:true,body:{reason:clean,detail:String(detail||"").trim()||undefined}});}
