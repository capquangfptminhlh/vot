import 'dotenv/config';import { ValidationPipe } from '@nestjs/common';import { NestFactory } from '@nestjs/core';import cookieParser from 'cookie-parser';import helmet from 'helmet';import { AppModule } from './app.module';
function validateEnv(){const required=['DATABASE_URL','REDIS_URL','JWT_ACCESS_SECRET','OTP_PEPPER','REFRESH_TOKEN_PEPPER','IP_HASH_PEPPER','SERIAL_HASH_PEPPER','S3_ENDPOINT','S3_ACCESS_KEY','S3_SECRET_KEY','S3_PRIVATE_BUCKET','S3_PUBLIC_BUCKET'];for(const k of required){if(!process.env[k])throw new Error(`${k} is required`)}for(const k of ['JWT_ACCESS_SECRET','OTP_PEPPER','REFRESH_TOKEN_PEPPER','IP_HASH_PEPPER','SERIAL_HASH_PEPPER'])if((process.env[k]||'').length<24)throw new Error(`${k} must be at least 24 characters`)}
async function bootstrap(){validateEnv();const app=await NestFactory.create(AppModule,{bufferLogs:true});const express=app.getHttpAdapter().getInstance();express.set('trust proxy',1);app.use(helmet());app.use(cookieParser());app.useGlobalPipes(new ValidationPipe({whitelist:true,forbidNonWhitelisted:true,transform:true}));const origins=(process.env.CORS_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean);if(process.env.NODE_ENV==='production'&&!origins.length)throw new Error('CORS_ORIGINS is required in production');app.enableCors({origin:origins.length?origins:false,credentials:true,methods:['GET','POST','PUT','PATCH','DELETE','OPTIONS']});app.setGlobalPrefix('api/v1');app.enableShutdownHooks();await app.listen(Number(process.env.PORT||3000),'0.0.0.0')}bootstrap();
