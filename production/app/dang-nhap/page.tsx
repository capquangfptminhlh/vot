'use client'
import { FormEvent, useState } from 'react'
import { createSupabaseBrowserClient, hasSupabaseEnv } from '@/lib/supabase/client'

export default function LoginPage(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(!hasSupabaseEnv()){setMsg('Backend Supabase chưa được cấu hình.');return}setBusy(true);const sb=createSupabaseBrowserClient();const {error}=await sb.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/marketplace`}});setBusy(false);setMsg(error?error.message:'Đã gửi link đăng nhập vào email.')}return <main><section className="pageHero"><div className="container"><span className="eyebrow">Tài khoản ChoVot</span><h1>Đăng nhập an toàn</h1><p>Đăng nhập bằng magic link. Sau khi đăng nhập, tài khoản có thể tiếp tục các bước xác thực người dùng.</p></div></section><section className="section"><div className="container" style={{maxWidth:560}}><form className="card" onSubmit={submit}><label htmlFor="email"><b>Email</b></label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="ban@example.com" style={{width:'100%',margin:'8px 0 16px',padding:'13px 15px',border:'1px solid var(--line)',borderRadius:14,font:'inherit'}}/><button className="btn primary" disabled={busy}>{busy?'Đang gửi...':'Gửi link đăng nhập'}</button>{msg&&<p>{msg}</p>}<p style={{fontSize:13,color:'var(--muted)'}}>Production sẽ bổ sung SĐT OTP và nhà cung cấp eKYC. Không lưu CCCD thô trong frontend.</p></form></div></section></main>}
